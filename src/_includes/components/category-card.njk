<a href="/{{ card.slug }}/" class="category-card">
  <img src="{{ card.thumbnail }}" alt="{{ card.name }}" class="category-card__image">
  <div class="category-card__content">
    <h2 class="category-card__title">{{ card.name }}</h2>
  </div>
</a>